скрипты insert, update, delete можно заменить одним оператором merge, в который можно передавать сразу массив данных(
далее подготовить временную таблицуприемника(target) ОДНИМ ЗАПРОСОМ и в завимисоти от наличия данных в таблице можно выполнять операторы),
а не бегать в цикле(с блокировкой). 

так же на стороне 1с можно реализовать механизм кэшей выполнения (прямой, обратный) и их синхронизации между потоками

данная возможности не реализована в этом проекте.